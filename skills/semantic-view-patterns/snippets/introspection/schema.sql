-- Introspection: Schema
-- This snippet uses the multi_fact_table SV as its example target.
-- Run multi_fact_table/schema.sql and seed_data.sql first, then deploy the SV.

-- No new tables needed — introspection commands operate on existing objects.

-- Reference SV: SNIPPETS.PUBLIC.MULTI_CHANNEL_SV
-- Created by: multi_fact_table/semantic_view.sql
